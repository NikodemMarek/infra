apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photos-pvc
  namespace: immich
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: nfs
  resources:
    requests:
      storage: 200Gi
  volumeName: photos-pv
