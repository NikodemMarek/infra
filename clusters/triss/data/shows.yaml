apiVersion: v1
kind: PersistentVolume
metadata:
  name: shows-pv
spec:
  capacity:
    storage: 200Gi
  volumeMode: Filesystem
  accessModes:
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs
  nfs:
    server: roach
    path: /mnt/data/shows
