apiVersion: v1
kind: Service
metadata:
  name: longhorn-ui
  namespace: longhorn-system
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "longhorn"
spec:
  selector:
    app: longhorn-ui
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
