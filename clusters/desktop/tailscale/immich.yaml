apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: immich
spec:
  ingressClassName: tailscale
  targetNamespace: default
  defaultBackend:
    service:
      name: immich
      port:
        name: immich-http
