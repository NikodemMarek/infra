apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: migration-snapshot-immich-data
  namespace: default
spec:
  volumeSnapshotClassName: longhorn
  source:
    persistentVolumeClaimName: immich-data-pvc
---
apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: migration-snapshot-immich-ml
  namespace: default
spec:
  volumeSnapshotClassName: longhorn
  source:
    persistentVolumeClaimName: immich-ml-pvc
